export default (theme) => ({
    root: {
        height: 10,
        border: '2px solid white',
        width: '95%',
        marginBottom: theme.spacing(2),
    },
    barColorPrimary: {
        backgroundColor: `${theme.palette.taskBarGreen} !important`,
    },
});
