export default (theme) => ({
    root: {
        width: '100%',
        height: '100%',
        backgroundColor: theme.palette.black,
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
    },
});
